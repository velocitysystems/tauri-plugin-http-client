[alias]
lint-clippy = "clippy --workspace --all-targets --all-features -- -D warnings"
lint-fmt = "fmt --all -- --check"
fix-clippy = "clippy --workspace --all-targets --all-features --fix --allow-dirty --allow-staged -- -D warnings"
fix-fmt = "fmt --all"
